# model/dataset_b — weights live on Zenodo

Same as dataset_a: copy `models/dataset_b/*` from the Zenodo record into this folder.
Needs `feature_schema.json` (shipped here) + `preprocess.joblib` + `base_learners.pkl` + `nam_meta_learner.pt`.
