# DeepGVS — Python >= 3.9
#
# 安装（stacking 预测 + FASTA 查表）:
#   pip install -r requirements.txt
#
# PyTorch：若需 GPU，请按 https://pytorch.org 选择与 CUDA 匹配的 wheel，例如:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
#
# 基学习器权重在 sklearn 1.3.x 下导出，请保持 scikit-learn>=1.3,<1.4。
# 更高版本可能 unpickle 失败（code/model.py 含部分兼容补丁，仍建议版本一致）。
#
# 现场 Graph-Mamba（--protein-mode compute）需额外依赖，见 requirements-graph-mamba.txt
# 或 conda 环境 VFS（fair-esm、mamba-ssm、torch 2.1+cu118）。

# --- 核心：prediction / verify_model / FASTA+csv / dump_preprocess ---
numpy>=1.21.0,<2.0.0
pandas>=1.3.0
scipy>=1.9.0
scikit-learn>=1.3.0,<1.4.0
xgboost>=1.7.0,<3.0.0
joblib>=1.2.0
tqdm>=4.62.0
torch>=2.0.0
