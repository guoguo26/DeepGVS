# DeepGVS — manuscript environment: Python 3.10.18, PyTorch 2.1.0 (CUDA 11.8)
# Stacking inference: FASTA -> CDS 354-dim + Graph-Mamba 1792-dim -> 2146 -> base learners -> NAM
numpy==1.26.2
pandas>=1.3.0
scipy==1.10.1
scikit-learn==1.3.2
xgboost==3.0.5
joblib>=1.2.0
tqdm>=4.62.0
torch==2.1.0
pyyaml>=6.0

# --- online FASTA + PDB path only (Graph-Mamba protein embeddings) ---
# These compile CUDA kernels at install time and need a CUDA 11.8 toolkit + g++;
# they are intentionally NOT installed by default so that the CPU / merged-features
# path works everywhere. Install manually when you need the online path:
#   pip install fair-esm==2.0.0 mamba-ssm==1.2.2 causal-conv1d==1.2.2.post1 einops
